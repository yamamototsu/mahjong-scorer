# 麻雀スコアラー 引き継ぎ資料

最終更新: 2026-08-16
公開URL: https://widespreder.github.io/mahjong-scorer/
リポジトリ: `widespreder/mahjong-scorer`

---

## 新しいチャットでの始め方

**この資料を添付して、次の1行を送るだけです。**

```
麻雀スコアラーの開発を続けます。まず下記を実行して環境を復元してください。
curl -sf https://raw.githubusercontent.com/widespreder/mahjong-scorer/main/dev/setup.sh | bash
```

ソースもビルドツールもリポジトリの `dev/` に入っているので、
ファイルを添付する必要はありません。約1分で以下がすべて復元されます。

- 最新のソース `mahjong-scorer.jsx`（約8,500行）を `/mnt/user-data/outputs/` へ
- Babel（JSX→JS変換用）と React（動作検証用）を `npm install`
- ビルド・検証・アップロードの各スクリプトを `/home/claude/` へ
- 試しに1回ビルドして動作確認

### 反映のしかた

```bash
export GH_TOKEN='＜トークンをここに貼る＞'
bash /home/claude/deploy.sh "コミットメッセージ"
```

※ トークンはこのリポジトリには置けません（GitHubのシークレット検出で拒否されます）。
手元の引き継ぎ資料か、パスワード管理アプリから貼り付けてください。

構文チェック → ビルド → `index.html` と `dev/mahjong-scorer.jsx` の両方をpush、まで自動です。
ソースもリポジトリに同期されるので、次回の引き継ぎも常に最新から始まります。

**⚠ トークンは2026年8月22日で期限切れ。**
更新時は Fine-grained で「Only select repositories」に `mahjong-scorer` を指定し、
Contents を Read and write にすること（Public repositories モードでは書き込めません）。

---

## リポジトリの構成

```
index.html                 ← 公開されるアプリ本体（変換済みJSを埋め込み済み）
dev/mahjong-scorer.jsx     ← 編集するソース
dev/setup.sh               ← 環境復元（これ1つで全部揃う）
dev/deploy.sh              ← 検証+ビルド+アップロード
dev/compile.js             ← JSX→JS変換
dev/validate.js            ← 構文チェック
dev/build-html.py          ← HTML生成
dev/upload.py              ← GitHub Contents API でpush
```

---

## 動作確認のしかた

コンテナからCDNに繋がらないため、ローカル検証用HTMLを作ってPlaywrightで確認します。

```python
c = open('/mnt/user-data/outputs/index.html', encoding='utf-8').read()
for a, b in [
  ('https://cdnjs.cloudflare.com/ajax/libs/react/18.2.0/umd/react.production.min.js',
   'file:///home/claude/prev/node_modules/react/umd/react.production.min.js'),
  ('https://cdnjs.cloudflare.com/ajax/libs/react-dom/18.2.0/umd/react-dom.production.min.js',
   'file:///home/claude/prev/node_modules/react-dom/umd/react-dom.production.min.js'),
  ('<link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@400;600;700;800;900&display=swap" rel="stylesheet">', ''),
]:
    c = c.replace(a, b)
open('/home/claude/local.html', 'w', encoding='utf-8').write(c)
```

Playwright（390×844, device_scale_factor=2）でスクリーンショットを撮り、目視で確認する。
`setup.sh` が React を `/home/claude/prev` に入れてあるので、そのまま使えます。

---

## 変更してはいけない設計判断

### 起動速度
JSXの変換は**必ずビルド時**に行う。ブラウザでBabelを動かすと起動に約10秒かかる
（対処前9.93秒 → 対処後0.16秒）。`type="text/babel"` を復活させないこと。

### 卓上モードのレイアウト
- 卓は**正方形**（`aspect-ratio: 1/1`）で画面中央に配置
- 中の要素はすべて `cqmin` 単位（卓の一辺に対する比率）。
  固定pxに戻すと端末サイズによって枠同士が重なる
- パネル 38%×26%、卓のふちからの余白 3%、中央の枠 25%
- 対局中はページのスクロールを無効化し、ずれても中央に戻す
- 席の回転（🔄）は**反時計回り**

### UI方針
- ウィザードは1画面1問。複数項目を1画面に詰め込まない
- 「対局終了」は自然な終局時の結果画面にのみ置く（途中終了はルール確認モーダル内）
- 説明ボタンに「？」や括弧は付けない
- 枠と枠は必ず離す（接触させない）

### 用語
カタカナは **アガリ / ホウジュウ** のみ。
連荘・流局・役満・満貫・本場・供託などは漢字のまま。

---

## 実装済み機能

### 対局メニュー
1. 東風戦・半荘戦（1回きり）
2. リーグ戦（複数回の通算成績）
3. 1局戦（翻・符から点数計算）
4. 対局履歴

### セットアップ（参加者 → 席決め → 形式 → ルール → 確認）
- **グループ**: 4人の組み合わせを保存してワンタップ呼び出し
- **席の並べ替え**: 「席を1つずらす」「ランダムに並べ替え」
- **前回と同じルールで始める**: 確認画面まで一気に飛ぶ
- 確認画面は**卓の配置**で座る位置を表示

### 卓上モード
- 4人のパネルが実際の座席位置に配置（対面は180度回転）
- リーチ宣言で即1,000点減算・供託に加算
- アガリ入力: あがった人（最大3人）→ ツモ/ロン → 翻 → 符 → 確認
- 複数ロンがルールを超えるとモーダルで確認（その場でルール変更も可）
- 流局: テンパイ選択、ノーテン罰符3,000点、連荘判定
- 点数を**長押し**で変動履歴（内訳を項目別に色分け）
- サイコロ、局・本場・供託の表示、画面スリープ防止
- 局の修正: アガリも流局も後から修正可（本場・供託は維持）

### ルール設定（設定 / セットアップ / リーグ の3か所で共通）
流局時の親（3択）、複数ロン（3択）、食いタン、後付け、切り上げ満貫、
オーラス親トップで終了、トビで終了、持ち点・返し点、ウマ（5種）。
「ウマ・オカとは」「それぞれのルールの説明を見る」の解説パネル付き。

### リーグ戦
名前・メンバー（4人以上）・終わり方（回数2〜100 / 期間）・ウマオカ・全ルール。
形式は対局ごとに選択。メンバーが4人ちょうどなら全員が自動選択。
pt計算は 五捨六入 → オカ → ウマ。同点は起家に近い席が上位。

### 練習問題
用語問題集（88語）／役一覧／飜数・役名テスト／符計算講座／
点数問題集（4翻以下160パターン）／点数早見表

### 符計算ガイド
- リーチ済みなら「鳴きましたか？」を省略して門前で計算
- 鳴きありなら平和・七対子を非表示、代わりにクイピン形のショートカット（30符固定）
- 役ピッカー: リーチ自動チェック、ツモなら門前清自摸和も自動、ドラ・裏ドラを別カウント

---

## localStorage キー一覧

| キー | 内容 |
|---|---|
| `mj_preset_names` | 登録プレイヤー名 |
| `mj_groups` | グループ（4人の組み合わせ） |
| `mj_default_rules` | ルールの初期値 |
| `mj_last_rules` | 前回の対局で使ったルール |
| `mj_leagues` | リーグ戦データ |
| `mj_dice_hold` | サイコロ表示秒数 |
| `mj_keep_awake` | 画面スリープ防止 |
| `mj_mastered` / `mj_wrong` | 覚えた役 / 間違え回数 |
| `mj_term_mastered` / `mj_term_wrong` | 覚えた用語 / 間違え回数 |
| `mj_fu_cleared` | 符計算講座のクリア済みレベル |

対局の保留・履歴も localStorage に保存。

---

## 検証済みの内容

- **点数計算エンジン**: 3者照合で283マス一致、エッジケース25件合格
- **符計算**: クイピン形・平和・七対子の各経路を検算
- **ウマ・オカ**: 持ち点×返し点の全組み合わせ×ウマ5種でpt合計がゼロ
- **変動履歴**: 履歴の再現と実際の点数が4パターンで一致
- **五捨六入**: 1,280通りの組み合わせで合計ゼロ
- **符の選択肢**: 翻数的にありえない20符・25符を除外

---

## 未対応・検討中

- Googleドライブ連携（クライアントID取得の案内まで）
- リーグでの飛び賞、チップ
- 5人以上のリーグでの出場回数の平準化
- チョンボの罰符
- 責任払い（包）、途中流局、三家和、流し満貫
  — 現状は説明パネルで「自動処理していません」と明記
- 「翻」と「飜」の表記ゆれ（変動履歴のみ「飜」、他は「翻」）
