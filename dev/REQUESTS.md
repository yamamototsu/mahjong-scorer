# 進行状況ボード

依頼はチャットで受け付けます。**このファイルはユーザーが書く場所ではありません。**
チャットで言われた要望を監督がここに転記し、担当と進み具合を記録します。

---

## 運用ルール（ユーザー指示・常設）

**画面に手を入れたら、必ずレイアウト検査を通してからコミットすること。**

```bash
node dev/layout-check.js local.html [actions.js] [幅,幅,...]
```

検査項目: 重なり／枠から文字があふれ／親からのはみ出し／画面外へのはみ出し／
枠どうしの接触／ふちへの詰まり／極小文字(10px未満)／不自然な折り返し／
小さすぎるタップ領域(32px未満)。幅を省略すると 280/320/390/430px で検査。

---

## 対応中

（なし）

---

## 完了（第3次・2026-08-22〜23）

| 内容 | 担当 |
|---|---|
| リーチ・ロン・ツモをユーザー提供の収録音声に差し替え（再生不可の端末は読み上げで代用） | 監督 |
| 局の読み上げを追加（局・本場の変わり目に「東一局 一本場」を端末の音声で。オンオフ可） | 監督 |
| 三人麻雀の総点検（点数エンジン検算1,243件全一致・実機通し・リーグpt・席まわり） | ルール |
| 三麻: アガリ確認画面の本場加算が300点固定表示だったのを、ルールの単位（連盟600点）に修正 | ルール |
| 三麻: 結果画面「点数を直接修正」の合計チェックが4人固定で常に警告が出ていたのを修正 | ルール |
| 三麻: リーグ詳細のウマ表記が常に「10-20」・オカが×4固定だったのを修正 | ルール |
| 三麻: 卓上モードでアガリに3人全員を選べて先へ進めなくなる行き止まりを修正（2人まで） | ルール |
| 三麻: 「前回と同じルールで」が人数違いの前回ルールを適用しpt合計が崩れていたのを修正 | ルール |
| 三麻: 履歴に記録すると履歴画面がエラーで開けなくなるクラッシュを修正（保存済みも救済） | ルール |
| アガリ確認画面: 受け渡しの卓を最初から埋め込み表示（タップで全画面）。スコアに反映・最初から計算（旧キャンセル）を画面下に固定 | 監督 |
| 対局開始の演出に「東1局」を表示。リーグの対局一覧を順位順に | 監督 |
| 確認画面の決定ボタンを1行にして最下部へ固定（iOSでstickyがずれて透けたためfixedに） | 監督 |
| 受け渡しの卓の下に内訳（点数・本場・リーチ棒）を分けて表示。矢じりをさらに鋭く | 監督 |
| 起動画面に点滅する TAP TO START を追加し、タップ待ちに変更 | 監督 |
| チョンボ処理を追加（流局から入る。誰が→払う額を選ぶ→確定。局はやり直し・リーチ棒返却。記録だけも可。設定に計算あり/なしと既定額。局の修正対応） | 監督 |
| リーグ作成のウマ初期値を「なし（素点のみ）」に | 監督 |
| 履歴: 対局を選んで集計（pt合計で順位・素点合計・レート換算・参加数・トップ回数） | 監督 |
| 履歴が端末に保存されていなかった不具合を修正（メモリ上だけで、閉じると消えていた） | 監督 |
| 局の読み上げを収録音声19ファイルに差し替え（東南西北・1〜4局・オーラス・1〜10本場をつないで再生。不可時は読み上げ） | 監督 |
| チョンボ設定に人数付き表記（4,000×3 など）を追加。金額に0も選択可 | 監督 |
| 効果音の設定を「音の設定」に集約（サイコロ音・ロン・リーチ・ツモ、試聴＋オンオフ） | 監督 |
| サイコロの秒数設定と「この山を割る」の点滅を同期 | 監督 |
| レート説明文を「点数を違うレート単位で表示します。」に修正 | 監督 |
| リーグ設定から開いた名前画面の戻り先をリーグ設定に（初期の名前に戻すは削除） | 監督 |
| グループ登録を三人麻雀／四人麻雀それぞれに対応 | 監督 |
| ツモの声の読み方を選択制に（ツモ／ツモッ／つも／自摸、既定は「ツモッ」） | 監督 |
| 全ボタン・戻る・キャンセルの総点検（21シナリオ・全画面、JSエラーなし） | 第三者 |
| セットアップ「人数・試合形式・ルール」の← 戻るが白画面になる不具合を修正 | 第三者 |
| 名前画面のヘッダー← 戻るも、開いた場所（設定／リーグ設定／メンバー決定）へ戻すように修正 | 監督 |
| 結果画面: 「✓ 対局終了・履歴に記録して終了」（赤）に改名し、「🔁 対局終了・履歴を残して再試合」（青）を追加。再試合は記録→東風/半荘/全荘を選択→同じメンバー・ルールで親決めから | 監督 |
| 履歴一覧: 各カードに「詳細を見る」ボタンを追加。カード全体のタップで詳細を開くのはやめた（集計の選択タップはそのまま） | 監督 |
| 対局中のルール確認（📋）から音・サイコロ・画面の設定も開けるように（設定画面と共用・変更は即時保存） | 監督 |
| 「← 戻る」の二重表示を全ページ点検して解消。ヘッダーの戻るが階層に応じた行き先へ（リーグ設定→一覧、対局開始→リーグ詳細、レッスン→方式選択など） | 監督 |
| 点数問題集に「⚡ フラッシュ記憶」を追加（問題→2秒後に答え→自動で次へ。子ロン/子ツモ/親ロン/親ツモ/ランダム×順番/シャッフル、一時停止つき） | 監督 |
| 順位・点差ビューのレート換算行を pt 表記から点数表記に（+3pt → +3,000点） | 監督 |

## 完了（第2次・2026-08-21）

| 内容 | 担当 |
|---|---|
| 点数の受け渡し画面: 矢印の黒い縁取りを削除 | 監督 |
| 点数の受け渡し画面: 点数と「アガリ」の重なりを修正（枠2pxに中身11px） | 監督＋レイアウト |
| 起動時はタイトルだけを演出表示し、そのあとメニューを出す | 監督 |
| レイアウト自動検査ツールを作成し、常設の運用にした | 監督 |
| 対局メニューのルール設定: 区切り線が説明文の上を横切るのを修正 | レイアウト |
| 点数の受け渡し: 「親」バッジが名前の枠からはみ出すのを修正 | レイアウト |
| 非卓上の対局ビュー: 「ルール」が「ルー/ル」と折り返すのを修正 | レイアウト |
| 翻数・符数の選択: 幅280pxでマス目が画面外へ出るのを修正 | レイアウト |
| 役一覧・メンバー決定・リーグ作成: 小さすぎるタップ領域を拡大 | レイアウト |

レイアウト検査の指摘は **427件 → 338件**（増えた画面はゼロ）。

## 完了（第1次・2026-08-21）

| 内容 | 担当 |
|---|---|
| 席決めで牌の下の名前が別人になる | 第三者 |
| 三人麻雀のリーグ作成で「ウマ・オカとは」を開くとクラッシュ | ルール |
| 結果画面・リーグ集計に NaN が出る | 監督 |
| 三人麻雀の既定値40,000点が選べず持ち点が誤表示 | ルール |
| 説明パネルを開いてもすぐ閉じる | レイアウト |
| 三人麻雀でウマ・オカ説明が崩れる | ルール |
| 対局設定の確認画面で行末に「｜」だけが残る | レイアウト |
| 表記ゆれ「飜」を「翻」に統一（3箇所） | ルール |
| 説明を開くボタン・対局メニュー・役ピッカーの極小文字を拡大 | レイアウト |
| 卓上モードの供託表示に単位を追加、cqmin化 | 第三者＋監督 |
| ウマ説明の符号を統一（正の値のみ「+」） | 第三者＋監督 |

---

## 保留・要相談（ユーザーの判断待ち）

1. **設定を変えても、同じセッション中は1局戦に反映されない**
   切り上げ満貫をONにして保存しても、開き直すまで7,700点のまま。
2. **トグルは右端のスイッチしか反応しない**
   食いタン等で文字や行をタップしても切り替わらない。行全体を押せるようにするか？
   （スイッチ自体が48×26でタップ領域も小さめ。まとめて判断したい）
3. ~~リーグ戦画面に「← 戻る」が2つ~~ → **対応済み**（全ページの二重戻るを解消・第3次）
4. **設定画面のウマ・オカが直前の対局人数に引きずられる**
   四人用の既定ルールを編集していても、直前が三人麻雀だと三人用のプリセットが出る。
   → 「設定画面で三人用のウマも設定できるべきか」という設計判断が必要。
5. **セットアップ Step3 の「← 戻る」で空白画面**（ヘッダーの戻るで脱出は可能）
6. **切り上げ満貫ONで8,000点になっても「満貫」ラベルが出ない**
7. **点数問題集・符計算は切り上げ満貫OFF固定で出題**。誤りではないが注記があると親切。
8. **開門ガイドの「右から 10 つ目」が不自然**。「個目」等が自然。卓上モード内。
9. **始め方ガイド「山を割る」の牌下の数字が7px**、**メンバー決定の「仮」バッジが9px**
10. **用語辞書の「自摸」「放銃」が四人麻雀前提**の記述（三人麻雀では2人から）
11. **卓上モードの「リーチ」ボタンが小さい**（幅280pxで6.6px、タップ領域52×26）
    cqmin設計のため端末が小さいほど小さくなる。卓上パネルの文字比率の見直しが必要。
12. **非卓上の対局ビューで「アガリ入力」が幅280pxで3行に折り返す**
    ウィザードのモーダルの裏にしか出ないため優先度は低い。
13. **幅280pxでヘッダーの「卓上ポンづけ」が2行になる**（280pxのみ）
14. **リーグ詳細→設定タブ→「設定を変更する」のキャンセルがリーグ一覧に戻る**
    （詳細に戻る方が自然。フォームが新規作成と共用のため。ボタン点検で発見）
15. **卓上のアガリ入力: 手順②から← 戻るで手順①に戻ると選択した人が残る**
    （選び直し用ならこのままでよい。キャンセルは完全リセットで一貫。ボタン点検で発見）
16. **【三麻・中】北の抜きドラが未実装で、扱いの説明もない**
    点数入力上は「ドラとして翻数に含める」運用で回せるが、その一言がアプリ内のどこにもない。
    ルール確認か説明パネルへの注記追加を推奨（三麻点検で発見）
17. **【三麻・低】三麻でも「全荘戦（東南西北）」が選べる**（北場まで回る12局。流派差のため報告のみ）
18. **【三麻・低】三麻でもルール設定に「トリプルロンあり」が選べる**（実際には2人までしか選べず実害なし）
19. **【三麻・低】リーグ成績表の見出しが「1-2-3-4」固定**（三麻でも4位列が出る。データは0のまま）

## 参考情報（対応不要と判断）

- 点数長押しの順位・点差ビューで下位者の点差が緑色 → 別セッションの担当領域
- リーチ宣言で持ち点が0未満でもトビ判定は局の確定時まで行わない（設計どおり）
- レイアウト検査の誤検出: `lineHeight:1` の数字・絵文字（実際にははみ出さない）、
  横スクロール行の画面外、日本語の通常の折り返し
